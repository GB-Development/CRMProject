@model List<CrmContact>;

@{
    ViewData["Title"] = "Workflow / Contacts";
    Layout = "~/Views/Workflow/_LayoutWorkflow.cshtml";
}
<div class="content row-col-1 d-flex flex-column align-items-center m-5">
    
    <div class="d-flex col-12">
        <div class="col-4">
            
        </div>
        <h3 class="col-4" style="color: white; text-align: center;">@ViewData["Title"]</h3>
        <div class="col-4" style="text-align: center;">
            <a href="#" class="btn btn-success">Add New Contact</a>
        </div>
    </div>

    <div class="d-flex justify-content-center flex-wrap container m-3">
        
        @if (Model is null || Model.Count == 0)
        {
            <p class="fs-4" style="color: white;">Нет контактов в машей базе.</p>
        }
        else
        {
            foreach (var contact in Model)
            {
                <div class="card m-1" style="background: rgba(100,100,155, 0.55);">
                    <div class="card-body" style="color: white;">
                        <div style="position:absolute; top: 45px; left: 225px; background: #FFFFFF; width: 76px; height: 76px; border-radius: 50%;">
                            <div style="position:relative; top: 3px; left: 3px; background: #D9D9D9; width: 70px; height: 70px; border-radius: 50%;"></div>
                        </div>
                        <h5 class="card-title">@contact.FullName</h5>
                        <p class="card-text m-0">
                            Address: @contact.Address
                        </p>
                        <p class="card-text m-0">
                            Email: @contact.Email
                        </p>
                        <p class="card-text">
                            Phone: @contact.PhoneNumber
                        </p>
                        <div class="btn-group col-12" >
                            <a href="#" class="btn btn-primary col-6">Edit</a>
                            <a href="#" class="btn btn-danger col-6">Delete</a>
                        </div>
                    </div>
                </div>
            }
        }
    </div>
</div>